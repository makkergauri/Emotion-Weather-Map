# --- runtime ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
feedparser>=6.0.11
vaderSentiment>=3.3.2
pydantic>=2.6
python-dotenv>=1.0

# --- optional: HIGH_ACCURACY_MODE ---
# Uncomment both to enable the DistilBERT engine. Roughly 250MB of model weights
# on first run, so it stays opt-in rather than a default install.
# transformers>=4.40
# torch>=2.2

# --- dev ---
pytest>=8.0
pytest-asyncio>=0.23
