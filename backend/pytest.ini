[pytest]
# Lets the suite import `app.*` when pytest is run from backend/ without
# installing the package or scattering sys.path hacks through conftest.
pythonpath = .
testpaths = tests
asyncio_mode = auto
